using System;
using UnityEngine;

public enum Attribute
{
    Speed,
    ArmorClass,
    AttackBonus,
    MagicResistance, //temporary
    MaxHealth,
    MaxEnergy,
    EnergyRegen,
    // Add more as needed
}

public enum VehicleStatus
{
    Active,
    Destroyed,
}