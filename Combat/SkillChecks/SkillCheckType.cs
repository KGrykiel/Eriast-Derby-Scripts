namespace Assets.Scripts.Combat.SkillChecks
{
    /// <summary>
    /// Types of skill checks in the game.
    /// Each represents a different type of challenge or ability test.
    /// </summary>
    public enum SkillCheckType
    {
        None,      // No skill check (used for event cards with no check required)
        Mobility
    }
}
