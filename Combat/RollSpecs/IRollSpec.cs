namespace Assets.Scripts.Combat.RollSpecs
{
    /// <summary>
    /// Marker interface for roll specifications.
    /// Each implementation represents one distinct roll type and carries only the fields that type needs.
    /// </summary>
    public interface IRollSpec { }
}
